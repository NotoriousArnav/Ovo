{
  "name": "ovo",
  "version": "0.1.0",
  "private": true,
  "license": "GPL-3.0",
  "description": "Ovo — Smart Task Manager. Full-stack mobile application built with Expo React Native and Express.js.",
  "scripts": {
    "dev": "turbo dev",
    "dev:app": "turbo dev --filter=@ovo/mobile",
    "dev:backend": "turbo dev --filter=@ovo/backend",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "clean": "turbo clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.4.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
