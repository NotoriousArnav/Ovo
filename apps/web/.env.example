VITE_API_URL=https://ovo-backend.vercel.app

# Event Horizon OAuth (optional — button will always show, backend controls availability)
# No client-side EH config needed; the login button redirects to the backend which handles everything
