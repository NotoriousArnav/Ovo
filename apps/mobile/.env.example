# Ovo Mobile — Environment Variables
# Copy this file to .env and fill in the values

# Backend API URL
EXPO_PUBLIC_API_URL=http://localhost:3001

# Event Horizon OAuth — No client-side config needed.
# The mobile app opens the backend's /api/auth/eventhorizon/login endpoint,
# which handles the entire OAuth2 flow server-side.
